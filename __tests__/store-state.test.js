import storeState from "./src/js/store-state.js";

describe ("something", () => {

  // 

  //tests
  test("should create an empty currentState object", () => {
    expect(currentState()).toEqual({});
  });


});